# Example environment config (safe to commit)
# Copy to `.env` for local dev if you need overrides.

# Base path for GitHub Pages (repo name + trailing slash)
VITE_BASE=/development-pokemon-evolution-tree-explorer/
